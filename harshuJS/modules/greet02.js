const greetings = {
    firstGreeting: name => {
        return `Good Morning ${name}`;
    },
    secondGreeting: name => {
        return `Top of the Morning ${name}`;
    },
};

module.exports = greetings;
